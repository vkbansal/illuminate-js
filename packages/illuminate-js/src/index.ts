import {
    TokenObject,
    TokenTypes,
    tokenize,
    Definition,
    Plugin,
    addPlugin,
    highlight,
    getLanguage
} from './illuminate';

export { TokenObject, TokenTypes, tokenize, Definition, Plugin, addPlugin, highlight, getLanguage };
