import {
    TokenObject,
    TokenTypes,
    tokenize,
    Definition,
    Plugin,
    addPlugin,
    highlight
} from './illuminate';

export { TokenObject, TokenTypes, tokenize, Definition, Plugin, addPlugin, highlight };
